# Your secret API key - make this long and random!
# Example: generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your-secret-api-key-here

# Your Ring account credentials (for initial setup only)
RING_USERNAME=your-ring-email@example.com
RING_PASSWORD=your-ring-password

# Optional: specify which intercom to use if you have multiple
# Leave empty to use the first one found
INTERCOM_NAME=
